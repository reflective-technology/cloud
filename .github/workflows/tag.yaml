name: Auto Tagging
on:
  push:
    branches:
      - main
    paths:
      - "VERSION"
jobs:
  version:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      value: ${{ steps.get_version.outputs.value }}
      tagged: ${{ steps.tagged.outputs.tagged }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-tags: true
      - name: Get Version
        id: get_version
        run: |
          if [ -f VERSION ]; then
            version=$(cat VERSION)
          else
            echo "VERSION file not found, using default version 0.0.0"
            version="0.0.0"
          fi
          echo "value=${version}" >> "$GITHUB_OUTPUT"
      - name: "Check: if the version has corresponding git tag"
        id: tagged
        shell: bash
        run: |
          git show-ref --tags --verify --quiet -- "refs/tags/v${{ steps.get_version.outputs.value }}" &&
          echo "tagged=0" >> $GITHUB_OUTPUT ||
          echo "tagged=1" >> $GITHUB_OUTPUT
  tag:
    runs-on: ubuntu-latest
    needs: version
    if: needs.version.outputs.value != '0.0.0' && needs.version.outputs.tagged == '1'
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Push tag
        id: tag_version
        uses: reflective-technology/github-tag-action@d86631af8dd0eea19a0c631fb26129f831addfa1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          tag_prefix: "v"
          custom_tag: ${{ needs.version.outputs.value }}
      - name: Create Release
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd
        with:
          immutableCreate: true
          makeLatest: ${{ !contains(needs.version.outputs.value, 'rc') }}
          prerelease: ${{ contains(needs.version.outputs.value, 'rc') }}
          generateReleaseNotes: true
          skipIfReleaseExists: true
          tag: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}
